name: Auto Discover Tournaments (Bulletproof Debug)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  update-tournaments-test:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Sniff, Filter and Debug
        run: |
          import requests
          import json
          import re
          import time
          from datetime import datetime, timedelta
          from requests.adapters import HTTPAdapter
          from urllib3.util.retry import Retry

          now = datetime.now()
          today_dt = now.date()
          TEST_FILE = "tournaments-test.json"

          PREHEAT_DAYS = 14 
          EXPIRE_DAYS = 1   

          WHITELIST = ["LPL", "LCK"]
          BLACKLIST = ["Opening", "Promotion", "KeSPA", "Showmatch", "LCK CL", "LPLOL", "Secondary", "Qualifier", "Academy", "Division"]

          # 防 429 的鲁棒 Session
          def get_robust_session():
              session = requests.Session()
              retry_strategy = Retry(
                  total=5,
                  backoff_factor=2,
                  status_forcelist=[429, 500, 502, 503, 504],
                  allowed_methods=["GET"]
              )
              adapter = HTTPAdapter(max_retries=retry_strategy)
              session.mount("https://", adapter)
              return session

          def run_sniff():
              url = "https://lol.fandom.com/api.php"
              
              # 🛡️ 修复：在 SQL 层就把纯种的 Secondary 干掉，只放行 Primary 和 国际赛
              where_sql = f"(Year = '{now.year}' OR Year = '{now.year + 1}') AND DateStart IS NOT NULL AND (TournamentLevel = 'Primary' OR TournamentLevel = 'International' OR Region = 'International')"
              
              params = {
                  "action": "cargoquery", "format": "json", "tables": "Tournaments",
                  "fields": "Name, OverviewPage, DateStart, Date, TournamentLevel, Region, Year, IsQualifier, IsPlayoffs",
                  "where": where_sql, "order_by": "DateStart ASC", "limit": "400"
              }
              headers = {"User-Agent": "LoLStatsBulletproof/32.0"}

              print(f"🚀 [INIT] 启动抓取 | 预热: {PREHEAT_DAYS}天 | SQL防御已开启\n" + "="*50)

              session = get_robust_session()
              data = []
              
              # 🛡️ 应用层重试机制 (抓取 Fandom 恶心的 HTTP 200 假笑报错)
              MAX_ATTEMPTS = 4
              for attempt in range(1, MAX_ATTEMPTS + 1):
                  try:
                      resp = session.get(url, params=params, headers=headers, timeout=30)
                      resp.raise_for_status()
                      resp_json = resp.json()
                      
                      if "error" in resp_json:
                          err_info = resp_json["error"]
                          print(f"⚠️ [API假笑报错] 尝试 {attempt}/{MAX_ATTEMPTS} | 错误信息: {err_info}")
                          if "ratelimit" in err_info.get("code", "").lower() or "limit" in err_info.get("code", "").lower():
                              sleep_time = 5 * attempt
                              print(f"   💤 触发限流退避，等待 {sleep_time} 秒...")
                              time.sleep(sleep_time)
                              continue
                          else:
                              break
                              
                      data = resp_json.get("cargoquery", [])
                      if data:
                          print(f"📥 [API] 成功获取 {len(data)} 条底层数据。\n" + "-"*50)
                          break
                      else:
                          print(f"⚠️ [API] 返回数据为空。原始响应: {resp_json}")
                          break

                  except requests.exceptions.RequestException as e:
                      print(f"💥 [网络异常] 尝试 {attempt}/{MAX_ATTEMPTS} 失败: {e}")
                      time.sleep(5 * attempt)

              if not data:
                  print("❌ [致命] 所有重试均失败，程序终止。")
                  return

              raw_groups = {}
              print("🔍 [逐条体检开始]")
              for item in data:
                  t = item["title"]
                  name = t.get("Name", "")
                  ov = t.get("OverviewPage", "")
                  level = t.get("TournamentLevel", "")
                  region = t.get("Region", "")
                  y = t.get("Year", "")
                  is_playoffs = t.get("IsPlayoffs")
                  is_qualifier = t.get("IsQualifier")
                  
                  # ---------------------------------------------------------
                  # 🚧 强力安检闸口 (Debug 拉满)
                  # ---------------------------------------------------------
                  
                  # 1. 拦截预选赛 (底层字段)
                  if is_qualifier == "1":
                      # print(f"🚫 [拦截-预选赛] Name: {name}") # 如果嫌吵可以注释掉这句
                      continue 

                  # 2. 拦截次级联赛 (防漏网之鱼：Region是国际，但Level是Secondary)
                  if level == "Secondary":
                      print(f"🚫 [拦截-次级野鸡] 彻底击杀伪装者 | Name: {name} | Region: {region}")
                      continue

                  # 3. 拦截黑名单
                  if any(k.lower() in name.lower() for k in BLACKLIST):
                      print(f"🚫 [拦截-黑名单] 命中兜底词库 | Name: {name}")
                      continue

                  # 4. 判断身份 (国际赛 vs 目标赛区)
                  is_intl = (level == 'International' or region == 'International')
                  is_white = any(k.upper() in ov.upper() for k in WHITELIST)

                  if not (is_intl or is_white):
                      print(f"🚫 [拦截-非目标] 既非国际赛也不在白名单 | Name: {name} | Region: {region} | Level: {level}")
                      continue
                  
                  print(f"✅ [准入] 放行 | 身份: {'国际赛' if is_intl else '白名单'} | Name: {name}")

                  # ---------------------------------------------------------
                  # ✂️ 季后赛断尾逻辑
                  # ---------------------------------------------------------
                  clean_ov = ov
                  if is_playoffs == "1" and not is_intl:
                      anchors = [r'Split[\s_]\d+', r'Cup', r'Season_Play-In', r'Season_Playoffs', r'Versus']
                      for pattern in anchors:
                          match = re.search(f'^(.+?{pattern})', ov, re.IGNORECASE)
                          if match:
                              clean_ov = match.group(1)
                              print(f"   ✂️ [切尾] 识别到季后赛，执行溯源: {ov} -> {clean_ov}")
                              break

                  group_key = f"INTL_{name}" if is_intl else f"{clean_ov}_{y}"

                  try:
                      s_dt = datetime.strptime(t["DateStart"], '%Y-%m-%d').date()
                      e_dt = datetime.strptime(t["Date"], '%Y-%m-%d').date() if t.get("Date") else s_dt
                  except: continue

                  # ---------------------------------------------------------
                  # 🔗 聚合存入
                  # ---------------------------------------------------------
                  if group_key not in raw_groups:
                      raw_groups[group_key] = {
                          "ov": clean_ov, "year": y, "name": name, "is_intl": is_intl,
                          "start": s_dt, "end": e_dt
                      }
                  else:
                      old_end = raw_groups[group_key]["end"]
                      raw_groups[group_key]["start"] = min(raw_groups[group_key]["start"], s_dt)
                      raw_groups[group_key]["end"] = max(raw_groups[group_key]["end"], e_dt)
                      if e_dt > old_end:
                          print(f"   🔗 [延寿] {clean_ov} 吸收季后赛寿命，结束日延至 {e_dt}")

              print("\n" + "="*50 + f"\n🎯 [生命周期与排版 (统一预热{PREHEAT_DAYS}天)]")
              
              final_output = []
              for gk, v in raw_groups.items():
                  # 统一预热判定
                  if v["start"] > (today_dt + timedelta(days=PREHEAT_DAYS)):
                      print(f"⏳ [潜伏] {v['name']} | 开赛: {v['start']} (未到14天预热)")
                      continue
                  if today_dt > (v["end"] + timedelta(days=EXPIRE_DAYS)):
                      print(f"🗑️ [过期] {v['name']} | 结束: {v['end']} (已过保质期)")
                      continue

                  # Slug 生成
                  if v["is_intl"]:
                      slug_name = v["name"].lower().replace(str(v["year"]), "").replace("mid-season invitational", "msi").replace("world championship", "worlds").replace("esports world cup", "ewc").strip()
                      slug = f"{v['year']}-{re.sub(r'[^a-z0-9]+', '-', slug_name).strip('-')}"
                  else:
                      parts = v["ov"].split('/')
                      slug = f"{v['year']}-{parts[0].lower()}-{re.sub(r'[^a-z0-9]+', '-', parts[-1].lower()).strip('-')}"
                  
                  print(f"✨ [生成] Slug: {slug} | Date: {v['start']} ~ {v['end']}")
                  final_output.append({
                      "slug": slug, "overview_page": v["ov"],
                      "start_date": v["start"].strftime('%Y-%m-%d'), "end_date": v["end"].strftime('%Y-%m-%d')
                  })

              unique_res = sorted([dict(t) for t in {tuple(d.items()) for d in final_output}], key=lambda x: x['slug'])
              with open(TEST_FILE, "w", encoding="utf-8") as f:
                  json.dump(unique_res, f, indent=4, ensure_ascii=False)
              
              print("\n" + "="*50 + f"\n🎉 同步完成。最终写入 {len(unique_res)} 条数据。")

          if __name__ == "__main__":
              run_sniff()
        shell: python

      - name: Commit and Push
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🛡️ Debug: Strictly block Secondary level and restore detailed logs"
          file_pattern: 'tournaments-test.json'
